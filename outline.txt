require

    What the controller is great for
    require
    require and link
    parent require
    errors and optional require

ngModel and ngModelController

    requiring ngModelController into your directives
    things that can be done with ngModelCtrl
    $validators

$setViewValue and custom inputs

    explanation of $viewValue, $modelValue, parsers/formatters pipeline
    $render function
    how built-in html input elements implement ngmodel controller
    <input type="text">
    <input type="checkbox">
    <select>
    building your own custom inputs

